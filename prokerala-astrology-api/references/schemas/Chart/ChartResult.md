# ChartResult

Object representing the SVG representation of the requested birth chart.

**Type:** string

The response body is an SVG document (`<?xml ...?><svg ...>...</svg>`) returned as a string with content type `image/svg+xml`.
